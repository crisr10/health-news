<nav class="navbar navbar-default navbar-fixed-top">
      <a class="navbar-brand" >Mongoose Scraper</a>
      <ul class="nav navbar-nav">
        <li><a href="/home">Home </a></li>
        <li class="active"><a href="/savedArticles">Saved Articles<span class="sr-only">(current)</span></a></li>
      </ul>
</nav>
<div class="mainImage">
    <div id="centerTitle">
        <h1>Saved Articles</h1>
    </div>
</div>
<br>
<br>
{{!-- pannels for each article --}}
<div class="container">
{{#each articles}}
<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">{{this.title}}</h3>
    <div id="savedArticleButtons">
      <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#commentsModal" data="{{this._id}}" aria-hidden="true">ARTICLE NOTES</button>

      <!-- Modal -->
      <div id="commentsModal" class="modal fade" role="dialog">
        <div class="modal-dialog">

          <!-- Modal content-->
          <div class="modal-content">
            <div class="modal-header">
              <button type="button" class="close" data-dismiss="modal">&times;</button>
              <h4 class="modal-title">Article id: {{this.id}}</h4>
            </div>
            <div class="modal-body">

            <ul>
            {{#each comments}}
            <li>{{this.body}}</li>
            {{/each}}
            </ul>

              <input type="hidden" value={{this.id}} class="articleId"/>
              <textarea type="text" class="form-control commentBody" placeholder="Add comment here"></textarea>
            </div>
            <div class="modal-footer">
              <button type="submit" class="btn btn-default commentSubmit" data-dismiss="modal">Submit</button>
            </div>
          </div>

        </div>
      </div>

      <button type="submit" class="btn btn-danger deleteFromSaved" data="{{this._id}}">DELETE FROM SAVED</button>
    </div>
</div>
<div class="panel-body">
    {{this.link}}
</div>
</div>
{{/each}}
</div>
<br>
<br>
<br>
